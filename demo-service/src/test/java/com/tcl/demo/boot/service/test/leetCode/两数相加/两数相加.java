package com.tcl.demo.boot.service.test.leetCode.两数相加;

import com.google.common.base.Optional;
import com.tcl.demo.boot.service.test.leetCode.OneWayNode;
import org.junit.Test;

public class 两数相加 {
/*
    @Test
    public void code() {


    }


    private OneWayNode add(OneWayNode one, OneWayNode two) {

        OneWayNode newNode = new OneWayNode();

        long carry = 0L;

        while (one != null || two != null) {


            OneWayNode curNode = newNode.getNext();

            long oneValue = 0l;
            long twoValue = 0l;

            if (null != one) {

                oneValue = one.getValue();
                one = one.getNext();
            }

            if (null != two) {

                twoValue = two.getValue();
                two = two.getNext();
            }


            long add = Optional.fromNullable(oneValue).or(0L) + Optional.fromNullable(twoValue).or(0L) + carry;

            carry = add / 10;
            long remainder = add % 10;


        }


        if(carry > 0){


            // todo 再加一个
        }


    }
*/

}
